# Banking Transactions API Configuration

# Set to "1" or "true" to enable test mode (allows DELETE operations)
# Default: disabled (production mode)
TEST_MODE=0

# Path to transaction data CSV file
# In test mode, uses data/test_transactions_data.csv
# In production mode, uses data/transactions_data.csv
DATA_PATH=data/transactions_data.csv

# Logging level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO
